# Clock-In — 2026-02-26 — 1001-VIDEO AVATAR (Optimization & Integration)

## 1) Context
- **Why now:** Yesterday we achieved a major milestone: a working **XTTS-v2** voice cloning pipeline and a modular `avatar_tts` package. Now we need to harden the environment, optimize the heavy CPU/GPU load during inference, and bridge the gap between backend stability and frontend user experience.
- **Current state:**
  - `api_server/avatar_tts/` structure is ready with `app.py`, `tts_engine.py`, and `sadtalker_runner.py`.
  - Coqui TOS is bypassed; XTTS-v2 runs silently in headless mode.
  - Marcus Aurelio voice verified as reference.
- **Related docs:** `docs/clockouts/2026-02-25-avatar-tts.md`, `docs/clockouts/2026-02-25-voice-cloning-fixes.md`.

## 2) Objective
- **Primary goal:** Optimize the voice cloning performance and implement real-time progress feedback in the UI.
- **Success criteria (definition of done):**
  - [ ] XTTS-v2 inference time is measured and logged for benchmarking.
  - [ ] Audio-to-video path is verified using the cloner components.
  - [ ] Gradio `app.py` runs successfully on the local lab machine.
  - [ ] Initial progress bar logic is drafted for the frontend.

## 3) Phase 1 — Performance & Optimization
1. Run local benchmarks for `xtts_v2` cloning vs. standard TTS.
2. Investigate bottleneck in `sadtalker_runner.py` (CPU vs CUDA latency).
3. Implement `fastapi-limiter` or similar to prevent server crashes during concurrent jobs.

## 4) Phase 2 — Integration & UI
1. Verify `audio-to-video` endpoint can accept custom voice clones.
2. Link the backend `progress` status to the frontend polling mechanism.
3. Update `voices/voices.json` with more verified personas.

## 5) Phase 3 — Closure
1. Update `walkthrough.md` with performance metrics.
2. Create daily clockout.

## 6) Deliverables
- Optimized `tts_engine.py`.
- Working Gradio lab link.
- Proof of high-speed cloning generation.
