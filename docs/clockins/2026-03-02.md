# Clock-In — 2026-03-02 — 1001-VIDEO AVATAR (Rename, Move & Progress Bar)

## 1) Context
- **Why now:** Last session (2026-02-27) completed the video clip redesign, aspect ratio support, context menu, and trash delete. The frontend and backend are stable and deployed. Today we resume with the pending "Next Steps" from that clockout.
- **Current state:**
  - Backend running at `http://127.0.0.1:8000` (uvicorn `--reload`).
  - Frontend running at `http://localhost:5173` (Vite).
  - Context menu has: **Rename**, **Move**, **Trash** (delete works end-to-end).
  - Rename and Move are UI stubs — no backend action yet.
- **Related docs:** `docs/clockouts/2026-02-27-ui-overhaul.md`.

## 2) Objective
- **Primary goal:** Implement Rename and real-time progress polling for active jobs; optionally scaffold Move (folder/project organization).
- **Success criteria (definition of done):**
  - [ ] Rename modal opens from context menu, sends `PATCH /api/v1/jobs/{job_id}` to update the title in DB, and reflects the new name in the UI immediately.
  - [ ] Job cards poll `GET /api/v1/jobs/{job_id}` every 3–5 s while status is `processing`, updating the progress bar and status badge without a full page refresh.
  - [ ] (Stretch) Move: modal with folder/project selector scaffolded and wired to a backend endpoint.
  - [ ] All changes committed and pushed to `falarcon3398/talkflow-v2` (`main`).

## 3) Phase 1 — Rename Feature
1. Add `PATCH /api/v1/jobs/{job_id}` endpoint in `jobs.py` — accepts `{ "title": "..." }` body, updates DB record.
2. Add `videoApi.renameJob(jobId, title)` in `frontend/src/api/client.js`.
3. Build `RenameModal` component (input field + Save / Cancel buttons) in `App.jsx`.
4. Wire Rename option in `VideoClipCard` context menu → open modal → call API → update local state.

## 4) Phase 2 — Progress Bar Polling
1. Identify jobs with `status === "processing"` when clips load.
2. Add `useEffect` polling hook (3 s interval, cleared on unmount or status change) inside `VideoClipCard` or a shared hook.
3. Update progress bar width and status badge from the polled response.
4. Test with a live generation run to confirm real-time updates.

## 5) Phase 3 — Move (Stretch)
1. Design DB schema addition: `folder` or `project_id` column on `jobs` table (migration via Alembic or inline `ALTER TABLE`).
2. Add `PATCH /api/v1/jobs/{job_id}/move` endpoint.
3. Scaffold Move modal with a simple folder list (hardcoded or fetched).

## 6) Deliverables
- Working Rename flow (modal → API → UI update).
- Real-time progress bar polling for in-progress jobs.
- Git commit pushed to `main`.
