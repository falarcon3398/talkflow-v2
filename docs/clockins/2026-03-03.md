# Clock-In — 2026-03-03 — 1001-VIDEO AVATAR (Job Management & Live Updates)

## 1) Context
- **Why now:** Yesterday (2026-03-02) we successfully integrated the Live Call (WebRTC) MVP and improved the demo flow. However, job management features like **Rename** and **Move** were deferred, along with real-time progress polling for generating clips.
- **Current state:**
  - Backend has a `Live Call` router and standard `Jobs` router.
  - Video clips context menu has "Rename" and "Move" options but they are currently non-functional stubs.
  - Users must manualy refresh to see job progress updates.
  - `/live` testing interface is available but needs validation with live model weights.

## 2) Objective
- **Primary goal:** Fully implement Job Rename and Move (backend + frontend) and enable real-time dashboard updates via polling.
- **Success criteria (definition of done):**
  - [ ] **Rename**: `PATCH /api/v1/jobs/{job_id}` implemented. Rename modal in dashboard updates the job title in DB and UI.
  - [ ] **Move**: `PATCH /api/v1/jobs/{job_id}/move` (or similar) implemented to allow organizing clips (scaffolded).
  - [ ] **Polling**: Job cards automatically poll for status/progress if `processing`, updating the UI in real-time.
  - [ ] **Live Call**: Verified `/live` signaling and audio/video tracks with model weights.
  - [ ] All changes committed to `main`.

## 3) Phase 1 — Job Management (Backend)
1. Add `PATCH /api/v1/jobs/{job_id}` in `jobs.py` to allow updating the `params["title"]` or a new `title` field.
2. Add `PATCH /api/v1/jobs/{job_id}/move` logic (e.g., updating a `project_id` or `folder` tag).
3. Update `Job` model if necessary (e.g., adding a explicit `title` column if `params` isn't enough).

## 4) Phase 2 — Dashboard UI (Frontend)
1. Create `RenameJobModal` in `App.jsx` (similar to `AvatarRenameModal`).
2. Wire "Rename" action in context menu to open the modal.
3. Build `MoveJobModal` scaffold with a simple "Project" selector.
4. Implement polling hook in `VideoClipCard` (poll every 3-5s if status is `processing` or `queued`).

## 5) Phase 3 — Verification & Polish
1. Run a new generation job and watch the progress bar update automatically.
2. Rename a clip and verify it persists after refresh.
3. Test the `/live` interface signaling flow.

## 6) Deliverables
- Functional Rename/Move for video clips.
- Automated progress bars in the dashboard.
- Verified WebRTC Live Call flow.
- Git commit.
