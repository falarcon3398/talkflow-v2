# Clock-In — 2026-03-04 — 1001-VIDEO AVATAR (Job Management & Live Updates)

## 1) Context
- **Why now:** Yesterday (2026-03-03) we stabilized the WebRTC Live Call audio/video tracks. However, dashboard integration of these improvements and deferred job management features (Rename/Move/Polling) remain the top priorities.
- **Current state:**
  - Live Call audio/video is functional in the prototype but needs better integration into the main dashboard.
  - Video clip context menu has "Rename" and "Move" stubs.
  - Job status polling is not yet implemented, requiring manual refresh.

## 2) Objective
- **Primary goal:** Port Live Call improvements to the dashboard and implement robust Job Management features.
- **Success criteria (definition of done):**
  - [ ] **Dashboard Integration**: Port hardened `/live` playback logic to the main React dashboard.
  - [ ] **Job Polling**: Implement automated progress tracking in the dashboard UI.
  - [ ] **Job Rename**: Backend and frontend implementation for renaming video clips.
  - [ ] **Job Move**: Backend implementation (or scaffold) for organizing clips into folders/projects.
  - [ ] All changes committed and verified.

## 3) Phase 1 — Dashboard & Polling
1. Update React dashboard component to match the hardened signaling and playback logic from `livecall.js`.
2. Implement `useJobPolling` hook or similar mechanism in `VideoClipCard` to update status/progress every few seconds.

## 4) Phase 2 — Job Management
1. Add `PATCH /api/v1/jobs/{job_id}` in the backend for `title` updates.
2. Implement "Rename" modal and wire it to the context menu.
3. Backend scaffold for `PATCH /api/v1/jobs/{job_id}/move`.

## 5) Phase 3 — Verification
1. Verify real-time progress bar updates for new generation jobs.
2. Verify Rename/Move persistence.
3. Smoke test the dashboard Live Call interaction.

## 6) Deliverables
- Real-time job progress updates.
- Functional Rename/Move for clips.
- Dashboard-integrated Live Call fixes.
- Git commit.
