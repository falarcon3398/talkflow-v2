# Clock-In — 2026-03-05 — 1001-VIDEO AVATAR (Job Polling & Avatar Fixes)

## 1) Context
- **Why now:** Yesterday (2026-03-04) we resolved the avatar creation bug and confirmed that `JobRenameModal` and `MoveJobModal` are fully wired in the frontend (`App.jsx`) and `PATCH /api/v1/jobs/{job_id}` handles both `title` and `project_id` updates in the backend.
- **Current state:**
  - ✅ Avatar creation and display are functional.
  - ✅ Rename/Move modals exist in the UI and are connected to the backend.
  - ❌ Real-time job progress polling is **not yet implemented** — the dashboard requires a manual refresh to see status changes.
  - ❌ Live Call improvements (`livecall.js` hardened playback) have not been ported into the main React dashboard.

## 2) Objective
- **Primary goal:** Implement automated job polling so video generation progress updates live in the dashboard without a manual refresh.
- **Success criteria (definition of done):**
  - [ ] **Job Polling:** A `setInterval`-based or `useEffect` polling loop fetches `/api/v1/jobs/` every ~4 seconds while any job is `pending` or `processing`, and updates the `jobs` state in `App.jsx`.
  - [ ] **Progress Bar:** `VideoClipCard` reflects live `progress` and `status` fields from the polled data.
  - [ ] **Live Call Dashboard Integration:** Port the hardened playback logic from `livecall.js` (`muted=false`, `volume=1.0`) into the main dashboard's Live Call view.
  - [ ] All changes committed to `main`.

## 3) Phase 1 — Job Polling
1. Locate the `jobs` state and fetching logic in `App.jsx` (search for the jobs-related `useEffect`).
2. Add a polling loop that calls `GET /api/v1/jobs/` on a 4-second interval when any job is `pending` or `processing`.
3. Clear the interval automatically when all jobs reach a terminal state (`completed`, `failed`).

## 4) Phase 2 — Live Call Dashboard Integration
1. Open `livecall.js` and identify the hardened `ontrack` / playback handler.
2. Find the equivalent section in the main dashboard Live Call component in `App.jsx`.
3. Apply the same `muted = false`, `volume = 1.0` guarantees.

## 5) Phase 3 — Verification
1. Start a new video generation job and confirm the progress bar updates without a page refresh.
2. Confirm job status correctly transitions: `pending → processing → completed`.
3. Smoke test a Live Call from the dashboard to confirm audio plays correctly.

## 6) Deliverables
- Real-time job polling in the dashboard.
- Hardened Live Call playback ported to the main dashboard.
- Git commit tagged with today's date.
