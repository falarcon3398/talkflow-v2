# Clock-In — 2026-03-09 — 1001-VIDEO AVATAR (Batch Actions & Voice Management)

## 1) Context
- **Why now:** We are entering a new week after successful delivery of the Live Call MVP and 9:16 pipeline support. To handle scaling, the library needs organization (Batch Actions) and the Voice Cloning feature needs a first-class management UI.
- **Current state:**
  - ✅ **Live Call:** MVP is functional with stable WebRTC signaling.
  - ✅ **9:16 Pipeline:** Automated ffmpeg center-cropping is production-ready.
  - ✅ **Job Polling:** Real-time updates are implemented in the dashboard.
  - ❌ **Batch Processing:** Users still have to delete/move clips one by one.
  - ❌ **Voice Cloning:** Feature exists in the backend but lacks a dedicated frontend library.
  - ⚠️ **Git Hygiene:** Significant uncommitted changes from the previous week (Mar 4-6) need to be staged and committed.

## 2) Objective
- **Primary goal:** Implement Multi-Select Batch Processing for the video library and launch the "Voice Library" interface.
- **Success criteria:**
  - [ ] **Batch Selection:** Toggle "Select Mode" in the Library to perform bulk Trash/Move actions.
  - [ ] **Voice Library:** A new sidebar view to manage, rename, and preview cloned voices.
  - [ ] **MentorAI Demo:** Implement "Cursor Animation" and "Callout" components in Remotion.
  - [ ] **Git Sync:** Clear the technical debt of uncommitted changes from last week.

## 3) Phase 1 — Git Synchronization
1. Review and commit the Batch 1 (Live Call / 9:16) changes.
2. Ensure the environment is clean for the new features.

## 4) Phase 2 — Batch Actions Implementation
1. Add `selectionMode` state to `App.jsx`.
2. Update `VideoClipCard` to support a "selected" state with visual feedback (checkbox/border).
3. Implement `handleBulkAction` API calls (Move/Trash).

## 5) Phase 3 — Voice Library & Cloning UI
1. Create `VoiceLibraryView.jsx` in the frontend.
2. Implement a dedicated "Clone New Voice" modal with multi-file upload support.
3. Integrate voice selection into the standard Avatar creation flow.

## 6) Phase 4 — MentorAI Demo Components
1. Refine the Remotion scaffold in `frontend/demos/`.
2. Implement the `RealisticCursor` and `GlassCallout` components.

## 7) Deliverables
- Functional Batch Select/Action system in the dashboard.
- New "Voice Library" sidebar item and view.
- Animated cursor and callout components in the MentorAI demo project.
- Clean git history.
