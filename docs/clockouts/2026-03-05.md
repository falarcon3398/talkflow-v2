# Clock-Out — 2026-03-05 — 1001-VIDEO AVATAR (Job Polling & 9:16 Aspect Ratio)

## 1) Execution Summary
- **Primary Goal:** Implement automated job polling and support for vertical (9:16) video generation.
- **Outcome:** ✅ **SUCCESS.** Real-time polling is live, and the video pipeline now supports professional 9:16 cropping for social media (TikTok/Reels).
- **Key Challenges:** Fine-tuning the ffmpeg crop/scale logic to ensure subjects remain centered when converting 16:9 or square sources to 9:16.

## 2) Accomplishments

### 📊 Real-Time Job Polling
- **Dynamic Intervals (`App.jsx`):** 
    - Implemented a `setInterval` loop that polls `/api/v1/jobs/` every **3 seconds** if there are active (queued/processing) jobs.
    - Switches to a **10-second** idle interval otherwise to save resources.
    - Verified that progress bars update in real-time without manual refresh.

### 📐 9:16 Video Support
- **FFmpeg Pipeline (`orchestrator.py`):**
    - Added `apply_aspect_ratio` utility using ffmpeg filters.
    - **9:16 Logic:** Scales to height 1920 and crops the center 1080px to maintain vertical focus.
    - **16:9 Logic:** Standardizes output to 1920x1080.
- **Integration:** Integrated seamlessly into the `run_text_to_video_pipeline`.

### 📞 Hardened WebRTC Signaling
- **Track Binding Fix (`webrtc.py`):**
    - Standardized track addition to happen during the `set_offer` phase.
    - This ensures `aiortc` correctly binds tracks to the browser's negotiated transceivers, eliminating "Silence" issues.
- **Dashboard Port:** Applied hardened playback guarantees (`muted=false`, `volume=1.0`) to the dashboard's Live Call interface.

### 🎥 MentorAI Demo Project
- **Remotion Scaffold:** Created initial components and assets for the 30-second MentorAI demo in `frontend/demos/`.
- **Theme Extraction:** Implemented brand palette extraction from UI screenshots.

## 3) Technical Debt & Gotchas
- **Mac mmpose Crash:** MuseTalk's `mmpose` dependency continues to crash on Mac M-series chips. Forcing `photo.jpg` fallback for Live Call lipsync until pre-computed `.pkl` files are generated for all idles.

## 4) Next Steps
- [ ] **Batch Processing:** Implement multi-job selection for Move/Trash.
- [ ] **Voice Cloning UI:** Add dedicated interface for uploading voice samples.

## 5) Git Logs
- `feat: implemented real-time job status polling in dashboard`
- `feat: added 9:16 aspect ratio support with ffmpeg center-cropping`
- `fix: standardized WebRTC track binding in set_offer`
- `feat: scaffolded MentorAI demo project and assets`
- `doc: added clock-out for 2026-03-05`
